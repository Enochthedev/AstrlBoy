[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app alembic upgrade head && python main.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 60
